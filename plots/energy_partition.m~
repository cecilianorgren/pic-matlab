no02m = PIC('/Volumes/Fountain/Data/PIC/no_hot_bg_n02_m100/data_h5/fields.h5');

h(1) = subplot(1,3,1); % color map
h(2) = subplot(1,3,[2 3]); % energy spectra

twci_map = 24000;
p_ic = no02m.twclim(twci_map).p([3 5]); % pressure of cold ions
A = no02m.twclim(twci_map).A; % magnetic vector potential

isub = 1;
if 1 % color map of temperature
  hca = h(isub); isub = isub + 1;
  
end